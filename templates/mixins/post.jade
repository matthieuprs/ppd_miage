mixin post(post)
	if groups
		ul.groups
			each grp in groups
				if grp.name
					li= grp.name
	.post.type-post.status-publish.format-standard.hentry.category-uncategorized.entry
		h2.entry-title
			a(href='blog/post/'+post.slug) #{post.title}
		span.entry-author= 'écrit par ' + post.author.name.full
		.entry-content!= post.content.extended
